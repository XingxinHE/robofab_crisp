# Default recording manager configuration
# This file is required by crisp_gym's recording manager factory.

# Dataset configuration
repo_id: "example_dataset"
robot_type: "Franka"
resume: false
fps: 30
num_episodes: 3
push_to_hub: false

# System configuration
use_sound: true
queue_size: 64
writer_timeout: 10.0
